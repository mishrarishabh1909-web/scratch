import numpy as np

class TextProcessor:
    def process(self, payload):
        # Mock BioBERT tokenizer and inference + projection
        # Return 64-dim feature vector
        features = np.random.randn(64)
        return features
